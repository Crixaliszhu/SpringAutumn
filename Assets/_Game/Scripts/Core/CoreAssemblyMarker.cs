namespace SpringAutumn.Core
{
    /// <summary>
    /// 内核程序集标记类型。仅用于让 SpringAutumn.Core 程序集成立，
    /// 并供测试通过反射定位本程序集（验证其不依赖 UnityEngine）。
    /// </summary>
    public static class CoreAssemblyMarker
    {
        public const string AssemblyName = "SpringAutumn.Core";
    }
}
